//#yosoy =  titulo Geoanilytics

// Path: assets/js/gsap.js








/* let tl = gsap.timeline({
    scrollTrigger: {
        trigger: '#yosoy',
        start: 'top center',
        end: 'bottom center',
        scrub: true,
        markers: false,

    }
})
tl.to('#yosoy', {
    y: 0
}) */

/* const lenis = new Lenis()

lenis.on('scroll', (e) => {
    console.log(e)
})

function raf(time){
    lenis.raf(time)
    requestAnimationFrame(raf)
}

requestAnimationFrame(raf) */
